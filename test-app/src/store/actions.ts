export const UPDATE_USERS = (value: any) => {
  return {
    type: "CHANGE_GROUP",
    payload: value,
  };
};

export const CHANGE_LOCATION = (value: any) => {
  return {
    type: "CHANGE_LOCATION",
    payload: value,
  };
};
